---
phase: 01-data-safety-import-reliability
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/phase-01/import-integrity.test.js
  - js/utils/import-integrity.js
  - js/parsers/nubank-fatura.js
  - js/parsers/itau-fatura.js
autonomous: true
requirements:
  - DATA-01
user_setup: []
must_haves:
  truths:
    - "Reimporting a Nubank or Itaú bill only replaces rows from that same card source and billing month."
    - "Same-month Registrato/SCR data and other card-source data remain intact after a reimport."
    - "Duplicate bill rows from parser noise are dropped before persistence and surfaced as warnings."
    - "Newly imported card rows carry enough provenance metadata to support future safe replacement."
  artifacts:
    - path: "js/utils/import-integrity.js"
      provides: "Pure helpers for card-row fingerprinting, duplicate collapsing, scoped replacement planning, and quality summary generation"
      exports: ["buildLancamentoFingerprint", "dedupeImportedLancamentos", "planScopedReplacement", "buildImportQuality"]
    - path: "js/parsers/nubank-fatura.js"
      provides: "Nubank bill importer with source-scoped replacement and diagnostics"
      exports: ["importarNubankFatura"]
    - path: "js/parsers/itau-fatura.js"
      provides: "Itaú bill importer with source-scoped replacement and diagnostics"
      exports: ["importarItauFatura", "pareceFaturaItauPorNome"]
    - path: "tests/phase-01/import-integrity.test.js"
      provides: "Regression coverage for replacement scope and duplicate handling"
      contains: "same-source replacement, other-source preservation, legacy-row fingerprint cleanup"
  key_links:
    - from: "js/parsers/nubank-fatura.js"
      to: "js/utils/import-integrity.js"
      via: "fingerprint + replacement planning before delete/insert"
      pattern: "planScopedReplacement|dedupeImportedLancamentos|buildImportQuality"
    - from: "js/parsers/itau-fatura.js"
      to: "js/utils/import-integrity.js"
      via: "fingerprint + replacement planning before delete/insert"
      pattern: "planScopedReplacement|dedupeImportedLancamentos|buildImportQuality"
    - from: "js/parsers/*-fatura.js"
      to: "lancamentos store"
      via: "delete only planned ids, then bulkAdd rows with importSource/importFingerprint"
      pattern: "deleteItem\\('lancamentos'|bulkAdd\\('lancamentos'"
---

<objective>
Stop the verified month-wide reimport corruption in card bill imports and make duplicate handling explicit before any analytics work builds on top of these records.

Purpose: DATA-01 is the hard foundation for the roadmap. Card bill reimports must stop deleting unrelated same-month data, and parser duplicates must be removed in a traceable way.

Output: Tested import-integrity helpers plus updated Nubank/Itaú bill importers that delete only safe targets, stamp source metadata on rows, and return diagnostics for later UI use.
</objective>

<execution_context>
@C:/Users/Admin/.copilot/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/SUMMARY.md
@.planning/codebase/CONCERNS.md
@copilot-instructions.md
@js/parsers/nubank-fatura.js
@js/parsers/itau-fatura.js
@js/db.js

<interfaces>
From js/parsers/nubank-fatura.js:
```javascript
export async function importarNubankFatura(file, onProgress = () => {}) { ... }
```

From js/parsers/itau-fatura.js:
```javascript
export function pareceFaturaItauPorNome(fileName) { ... }
export async function importarItauFatura(file, onProgress = () => {}) { ... }
```

Current persistence pattern in both bill parsers:
```javascript
const todos = await getAll('lancamentos');
for (const item of todos) {
  if (item.fatura === mesFatura) {
    await deleteItem('lancamentos', item.id);
  }
}
await bulkAdd('lancamentos', lancamentos.map(item => ({
  ...item,
  fatura: mesFatura,
})));
```

Replace that month-wide delete with helper-driven planning. New rows must include stable provenance fields such as:
```javascript
{
  importSource: 'nubank-fatura' | 'itau-fatura',
  importPeriodKey: 'Mar/2026',
  importFingerprint: '...',
}
```
These extra fields must be additive only; do not change existing view contracts or move stores.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create tested import-integrity helpers for safe replacement and duplicate collapse</name>
  <read_first>
    - js/parsers/nubank-fatura.js
    - js/parsers/itau-fatura.js
    - js/db.js
    - .planning/codebase/CONCERNS.md
  </read_first>
  <files>tests/phase-01/import-integrity.test.js, js/utils/import-integrity.js</files>
  <behavior>
    - Test 1: same-month rows from another source are preserved when planning a Nubank or Itaú reimport.
    - Test 2: same-source rows for the same billing month are selected for replacement.
    - Test 3: legacy rows without importSource are only selected for deletion when their normalized fingerprint matches an incoming row.
    - Test 4: duplicate parsed rows inside one import batch collapse to a single persisted row and emit warning metadata.
    - Test 5: quality summary text produces deterministic badge data such as "28 transações · confiança 94%".
  </behavior>
  <action>
    Create a pure helper module `js/utils/import-integrity.js` with no DOM, FileReader, or IndexedDB side effects so it can run under `node --test --experimental-default-type=module`.

    Implement helpers for Phase 1 card imports only:
    - `normalizeImportText(value)` for accent-insensitive, case-insensitive matching.
    - `buildLancamentoFingerprint(item)` using normalized date, description, value, parcela, and source family inputs.
    - `dedupeImportedLancamentos(items)` returning `{ uniqueItems, warnings, duplicateCount }`.
    - `planScopedReplacement({ existingItems, incomingItems, sourceKey, periodKey })` returning exactly which ids are safe to delete.
    - `buildImportQuality({ importedCount, duplicateCount, warningCount, unitLabel })` returning stable badge/score metadata for later UI rendering.

    Keep the helper additive and card-import specific; do not introduce Dexie, a backend, or a schema migration framework.
  </action>
  <verify>
    <automated>node --test --experimental-default-type=module tests/phase-01/import-integrity.test.js</automated>
  </verify>
  <acceptance_criteria>
    Helper tests prove that same-month other-source rows survive, same-source rows are replaced, and duplicate rows are collapsed with warnings.
  </acceptance_criteria>
  <done>
    `js/utils/import-integrity.js` exists with tested pure helpers, and the tests document the exact replacement/duplicate rules for DATA-01.
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply safe replacement and diagnostics to Nubank and Itaú bill parsers</name>
  <read_first>
    - js/utils/import-integrity.js
    - js/parsers/nubank-fatura.js
    - js/parsers/itau-fatura.js
    - js/views/importar.js
  </read_first>
  <files>js/parsers/nubank-fatura.js, js/parsers/itau-fatura.js</files>
  <action>
    Update both card-bill parsers to use the new helper module before persistence.

    In each parser:
    - Assign a fixed `sourceKey` (`nubank-fatura` or `itau-fatura`).
    - Compute `importFingerprint` for each parsed row.
    - Deduplicate the parsed batch before writing.
    - Replace the current `item.fatura === mesFatura` blanket delete with `planScopedReplacement(...)`.
    - Delete only the ids returned by the helper.
    - Persist new rows with additive provenance fields: `importSource`, `importPeriodKey`, `importFingerprint`.
    - Preserve existing fields used by the dashboard (`data`, `fatura`, `desc`, `cat`, `canal`, `valor`, `parcela`, `totalCompra`).

    Also extend the success return shape so later UI work can consume it without guessing:
    - `quality: { score, label, unitLabel, importedCount, warningCount }`
    - `warnings: [{ code, message, sample?, level }]`

    Capture at least these warning sources:
    - duplicate rows dropped from the same parsed batch
    - legacy-row cleanup ambiguity if any old same-month rows lacked `importSource` and could not be safely auto-replaced
    - parser candidates that looked transaction-like but failed final validation, capped to a small list plus overflow count

    Do not touch extrato or Registrato stores here. Do not rework layout detection in `layout-profiles.js` in this plan.
  </action>
  <verify>
    <automated>node --test --experimental-default-type=module tests/phase-01/import-integrity.test.js</automated>
  </verify>
  <acceptance_criteria>
    Reimport logic in both bill parsers uses helper-planned deletion instead of month-wide deletion, and successful results expose `quality` plus `warnings`.
  </acceptance_criteria>
  <done>
    Reimporting a card bill can no longer wipe unrelated same-month data, and duplicate-card rows are removed before persistence with diagnostics available for the import UI.
  </done>
</task>

</tasks>

<verification>
Run the task test suite:
- <automated>node --test --experimental-default-type=module tests/phase-01/import-integrity.test.js</automated>

Then manually confirm in browser after execution:
1. Import a Registrato/SCR file for a month already represented in the app.
2. Reimport a Nubank or Itaú bill for that same month.
3. Confirm card rows refresh but Registrato/SCR counts remain intact.
</verification>

<success_criteria>
- DATA-01 card-bill reimports no longer use month-only deletion.
- Card rows persist additive provenance metadata for future safe replacement.
- Duplicate parsed rows are collapsed with warnings instead of silently stored twice.
- Parser success payload now contains diagnostics the Importar tab can render directly.
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-safety-import-reliability/01-01-SUMMARY.md`
</output>
