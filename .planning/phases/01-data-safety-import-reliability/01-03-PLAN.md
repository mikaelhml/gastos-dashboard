---
phase: 01-data-safety-import-reliability
plan: "03"
type: execute
wave: 2
depends_on:
  - "01"
  - "02"
files_modified:
  - tests/phase-01/import-feedback.test.js
  - js/utils/import-feedback.js
  - js/views/importar.js
  - js/parsers/nubank-conta.js
  - js/parsers/itau-conta.js
  - js/parsers/registrato-scr.js
  - index.html
  - css/styles.css
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
user_setup: []
must_haves:
  truths:
    - "Each import result card shows a trust badge with imported count and confidence text."
    - "A user can expand a warnings panel per file to see parser caveats or skipped rows."
    - "A user can export a full JSON backup from the existing Importar tab."
    - "A user can restore a validated full backup from the Importar tab and immediately see the dashboard repopulated."
    - "The existing config-only JSON flow remains available as a separate, clearly labeled tool."
  artifacts:
    - path: "js/utils/import-feedback.js"
      provides: "Pure view-model helpers for quality badge text, warning rendering, and result-card states"
      exports: ["buildImportResultViewModel", "buildWarningsViewModel"]
    - path: "js/views/importar.js"
      provides: "Import tab wiring for trust feedback and full backup/restore actions"
      exports: ["buildImportar", "clearBase", "clearAllDashboardData"]
    - path: "index.html"
      provides: "Import tab markup for full backup controls and warnings toggles"
      contains: "fullBackupExportBtn"
    - path: "css/styles.css"
      provides: "Styles for quality badges, warnings accordion, and backup/restore panel states"
      contains: ".import-warning-panel"
  key_links:
    - from: "js/views/importar.js"
      to: "js/utils/full-backup-io.js"
      via: "export/preview/apply button handlers"
      pattern: "exportFullBackup|readFullBackup|applyFullBackupRestore"
    - from: "js/views/importar.js"
      to: "parser result objects"
      via: "quality + warnings rendering in import result cards"
      pattern: "resultado\\.quality|resultado\\.warnings"
    - from: "index.html"
      to: "js/views/importar.js"
      via: "new backup button ids and hidden file input"
      pattern: "fullBackupExportBtn|fullBackupImportBtn|fullBackupFileInput"
---

<objective>
Expose Phase 1 trust and recovery features in the existing Importar tab without changing the stack or breaking current config import/export.

Purpose: Phase 1 is only complete when the user can see import quality feedback in the current flow and can export/restore a full backup from inside the app.

Output: A wired Importar tab with full backup/restore controls, per-file quality badges, expandable warnings, and default diagnostics from the remaining importers.
</objective>

<execution_context>
@C:/Users/Admin/.copilot/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/SUMMARY.md
@.planning/codebase/CONCERNS.md
@index.html
@css/styles.css
@js/views/importar.js
@js/utils/config-io.js
@js/parsers/nubank-conta.js
@js/parsers/itau-conta.js
@js/parsers/registrato-scr.js
@.planning/phases/01-data-safety-import-reliability/01-01-SUMMARY.md
@.planning/phases/01-data-safety-import-reliability/01-02-SUMMARY.md

<interfaces>
From 01-01, successful importer results must now support:
```javascript
{
  importado: Number,
  duplicata: Boolean,
  mes: String,
  erro?: String,
  debug?: String,
  quality?: {
    score: Number,
    label: String,
    unitLabel: String,
    importedCount: Number,
    warningCount: Number
  },
  warnings?: [
    { code: String, message: String, sample?: String, level?: 'info' | 'warning' }
  ]
}
```

From 01-02, full backup module must expose:
```javascript
export async function exportFullBackup() { ... }
export async function readFullBackup(file) { ... }
export async function applyFullBackupRestore(payload) { ... }
```

Existing config-only functions must remain available:
```javascript
export async function exportConfig() { ... }
export async function importConfig(file) { ... }
```

Keep these as separate user actions with separate copy so the user can tell "backup completo" from "configuração manual".
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create tested import-feedback view-model helpers</name>
  <read_first>
    - js/views/importar.js
    - css/styles.css
    - index.html
    - .planning/phases/01-data-safety-import-reliability/01-01-PLAN.md
  </read_first>
  <files>tests/phase-01/import-feedback.test.js, js/utils/import-feedback.js</files>
  <behavior>
    - Test 1: a successful import with diagnostics renders a stable badge label such as "28 transações · confiança 94%".
    - Test 2: duplicate and parse-error states map to distinct result-card statuses.
    - Test 3: empty warnings render a neutral "Sem avisos" view model instead of a blank panel.
    - Test 4: long warning lists are truncated into a concise expandable view model with overflow count.
  </behavior>
  <action>
    Create a pure helper module `js/utils/import-feedback.js` that transforms importer results into UI-ready metadata without touching the DOM directly.

    Include helpers for:
    - result-card status/icon/text selection
    - quality badge label fallback when an importer returns no diagnostics yet
    - warnings list normalization
    - compact/expanded warning presentation

    Keep the output simple so `js/views/importar.js` can render with existing string-template patterns instead of introducing a framework or component system.
  </action>
  <verify>
    <automated>node --test --experimental-default-type=module tests/phase-01/import-feedback.test.js</automated>
  </verify>
  <acceptance_criteria>
    The import feedback helper has tests covering badge copy, warning states, and fallback behavior for importers with minimal diagnostics.
  </acceptance_criteria>
  <done>
    `js/utils/import-feedback.js` exists with tested logic for rendering trust feedback consistently across import result cards.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire full backup/restore and trust feedback into the Importar tab</name>
  <read_first>
    - js/utils/full-backup-io.js
    - js/utils/import-feedback.js
    - js/views/importar.js
    - index.html
    - css/styles.css
    - js/utils/config-io.js
    - js/parsers/nubank-conta.js
    - js/parsers/itau-conta.js
    - js/parsers/registrato-scr.js
  </read_first>
  <files>js/views/importar.js, js/parsers/nubank-conta.js, js/parsers/itau-conta.js, js/parsers/registrato-scr.js, index.html, css/styles.css</files>
  <action>
    Update the existing Importar flow in-place; do not move this work to a new tab.

    In `index.html` and `css/styles.css`:
    - add a dedicated "Backup completo da base" panel with:
      - `fullBackupExportBtn`
      - `fullBackupImportBtn`
      - hidden `fullBackupFileInput`
      - result/feedback area distinct from config-only feedback
    - keep the existing config panel, but relabel it clearly as config/manual data only
    - add a compact warnings accordion/panel style for each import result card
    - add quality badge styling that fits the existing badge/card visual language

    In `js/views/importar.js`:
    - import and use `exportFullBackup` / `readFullBackup` / `applyFullBackupRestore`
    - keep `exportConfig` / `importConfig`
    - bind the new backup buttons and hidden file input
    - show a restore preview summary after `readFullBackup(file)`
    - show a destructive confirmation before calling `applyFullBackupRestore(payload)`
    - after successful full restore, call `refreshStatus()` and `window.refreshDashboard?.()`
    - render each import result card with:
      - status icon and status text
      - quality badge (`resultado.quality.label` or helper fallback)
      - expandable warnings panel using the helper module
      - existing period/detail text preserved

    In the remaining importers (`nubank-conta.js`, `itau-conta.js`, `registrato-scr.js`):
    - return the same shared `quality` and `warnings` fields as the card-bill parsers
    - include parser-specific warning entries even when row-level diagnostics are limited, so every imported file has a non-empty trust payload (`warnings` or explicit `Sem avisos`)
    - include useful info such as page count or snapshot count for Registrato in details, but keep `warnings` as a list structure the UI can render
    - do not change their existing persistence scope logic in this plan

    Keep everything additive and local-first. No backend, no stack swap, no package-manager dependency.
  </action>
  <verify>
    <automated>node --test --experimental-default-type=module tests/phase-01/import-feedback.test.js &amp;&amp; powershell -Command "Select-String -Path 'index.html','js/views/importar.js','css/styles.css' -Pattern 'fullBackupExportBtn|fullBackupImportBtn|Sem avisos|confiança|import-warning-panel' | Out-Null"</automated>
  </verify>
  <acceptance_criteria>
    The Importar tab contains separate full-backup and config-only controls, successful imports show a quality badge, warnings are expandable per file, and a successful restore refreshes the dashboard immediately.
  </acceptance_criteria>
  <done>
    The user can see import trust feedback in the existing import log and can export/restore a full JSON backup from the app without losing the older config-only workflow.
  </done>
</task>

</tasks>

<verification>
Run:
- <automated>node --test --experimental-default-type=module tests/phase-01/import-feedback.test.js</automated>

Then verify in browser:
1. Open `📥 Importar`.
2. Confirm a distinct "Backup completo da base" action pair exists alongside the existing config-only tools.
3. Import one supported PDF and confirm the result card shows a quality badge plus expandable warnings.
4. Export a full backup, clear data in a disposable browser profile, restore the backup, and confirm transactions, subscriptions, and settings repopulate.
</verification>

<success_criteria>
- DATA-01 trust feedback is visible in the current import UI.
- DATA-02 full-dataset export is available from inside the app.
- DATA-03 validated restore is available from inside the app and refreshes the dashboard after success.
- Existing config-only JSON import/export still exists and is clearly separated from full backup/restore.
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-safety-import-reliability/01-03-SUMMARY.md`
</output>
