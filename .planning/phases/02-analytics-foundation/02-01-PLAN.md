---
phase: 02-analytics-foundation
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - js/utils/analytics.js
  - tests/phase-02/analytics-months.test.js
  - tests/phase-02/analytics-aggregation.test.js
  - tests/phase-02/analytics-delta.test.js
autonomous: true
requirements:
  - ANLY-01
  - ANLY-02

must_haves:
  truths:
    - "Imported card and account transactions can be reduced into month-by-month category spend totals without double counting account-side bill payments."
    - "Month ordering is chronological across Jan/2026-style labels, so trend and delta logic compare the correct adjacent months."
    - "Month-over-month category movers expose increases, decreases, and new/zeroed categories in a deterministic order."
    - "Analytics quality remains honest: high 'Outros' share is measurable and available to the UI instead of being hidden."
  artifacts:
    - path: "js/utils/analytics.js"
      provides: "Pure analytics helpers for month normalization, spend filtering, aggregation, movers, and quality notes"
      exports:
        - "monthLabelToKey"
        - "sortMonthLabels"
        - "normalizeAnalyticsTransactions"
        - "isSpendTransaction"
        - "aggregateMonthlyCategoryTotals"
        - "computeMonthOverMonthDeltas"
        - "buildSpendAnalytics"
    - path: "tests/phase-02/analytics-months.test.js"
      provides: "Regression coverage for month parsing and chronological sorting"
    - path: "tests/phase-02/analytics-aggregation.test.js"
      provides: "Regression coverage for merged spend aggregation and bill-payment exclusion"
    - path: "tests/phase-02/analytics-delta.test.js"
      provides: "Regression coverage for month-over-month delta ordering and display semantics"
  key_links:
    - from: "tests/phase-02/analytics-aggregation.test.js"
      to: "js/utils/analytics.js"
      via: "aggregateMonthlyCategoryTotals and buildSpendAnalytics imports"
      pattern: "from '../../js/utils/analytics.js'"
    - from: "js/utils/analytics.js"
      to: "account-side Fatura Crédito exclusion"
      via: "isSpendTransaction"
      pattern: "item\\.source === 'conta'.*item\\.cat === 'Fatura Crédito'"
    - from: "js/utils/analytics.js"
      to: "month-over-month movers"
      via: "computeMonthOverMonthDeltas"
      pattern: "delta"
---

<objective>
Lock the analytics contracts before any UI work by building pure helpers and node:test coverage for unified monthly category trends and movers.

Purpose: De-risk Phase 2 by proving the spend semantics first, especially the locked constraint to exclude account-side `Fatura Crédito` from totals and the requirement to keep category-quality caveats visible.

Output: A tested `js/utils/analytics.js` module that later plans can wire into the existing Lançamentos render pipeline without inventing logic inside the view.
</objective>

<execution_context>
@C:/Users/Admin/.copilot/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/SUMMARY.md
@.planning/phases/02-analytics-foundation/02-RESEARCH.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@copilot-instructions.md
@js/app.js
@js/views/lancamentos.js
@js/views/extrato.js
@js/utils/categorizer.js
@tests/phase-01/import-integrity.test.js

<interfaces>
From js/app.js:
```javascript
initLancamentos(lancamentos, extratoTransacoes, assinaturas, despesasFixas, {
  cardBillSummaries,
  registratoContextRows,
});
```

From js/views/lancamentos.js:
```javascript
const extratoNorm = extratoTransacoes.map(t => ({
  ...t,
  _origId: t.id,
  id: 'ext_' + (t.id ?? Math.random()),
  fatura: t.mes,
  source: 'conta',
}));

const cartaoNorm = lancamentos.map(l => ({ ...l, source: 'cartao' }));
```

From js/utils/categorizer.js:
```javascript
export function categorizar(desc) { /* returns category or 'Outros' */ }

// Important category for Phase 2 semantics:
'Fatura Crédito'
'Outros'
```

From tests/phase-01/import-integrity.test.js:
```javascript
import test from 'node:test';
import assert from 'node:assert/strict';
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing analytics contract tests</name>
  <files>tests/phase-02/analytics-months.test.js, tests/phase-02/analytics-aggregation.test.js, tests/phase-02/analytics-delta.test.js</files>
  <behavior>
    - Test 1: `sortMonthLabels()` orders `Dez/2025`, `Jan/2026`, `Fev/2026`, `Mar/2026` chronologically instead of lexicographically.
    - Test 2: `aggregateMonthlyCategoryTotals()` merges `lancamentos.fatura` with `extrato_transacoes.mes` into one month axis.
    - Test 3: Account-side rows with `source === 'conta'`, `tipo !== 'saida'`, or `cat === 'Fatura Crédito'` are excluded from spend totals per locked phase constraint.
    - Test 4: Any row flagged as `contextoDerivado` is excluded from analytics totals.
    - Test 5: `computeMonthOverMonthDeltas()` sorts movers by absolute delta and preserves `previous`, `current`, `delta`, and percent/null semantics.
    - Test 6: `buildSpendAnalytics()` emits an honest quality payload when `Outros` is a large share of analyzed spend instead of suppressing it.
  </behavior>
  <action>Create three node:test files under `tests/phase-02/` that import `../../js/utils/analytics.js` and define the exact spend semantics for Phase 2. Use fixtures that mix card rows, account rows, derived SCR rows, and `Fatura Crédito` account payments so the exclusion rule is explicit. Include at least one fixture where `Outros` is >= 25% of total analyzed spend and assert that the returned quality model exposes a warning/note. Keep the tests pure and deterministic; do not touch DOM or IndexedDB.</action>
  <verify>
    <automated>node --test tests/phase-02/analytics-months.test.js tests/phase-02/analytics-aggregation.test.js tests/phase-02/analytics-delta.test.js</automated>
  </verify>
  <done>All Phase 2 analytics expectations are codified in failing tests that clearly describe month ordering, spend inclusion/exclusion, movers ordering, and honest `Outros` reporting.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement pure analytics helpers until the tests pass</name>
  <files>js/utils/analytics.js</files>
  <behavior>
    - Helper output includes stable sorted months for imported data.
    - Aggregation uses card `fatura` and account `mes` without adding any IndexedDB store.
    - Account-side `Fatura Crédito` is excluded from spend totals to avoid double counting.
    - Movers compare the latest imported month against the immediately previous imported month.
    - `Outros` remains visible in totals/movers and surfaces a warning payload when category quality is weak.
  </behavior>
  <action>Create `js/utils/analytics.js` as a pure helper module with named exports: `monthLabelToKey`, `sortMonthLabels`, `normalizeAnalyticsTransactions`, `isSpendTransaction`, `aggregateMonthlyCategoryTotals`, `computeMonthOverMonthDeltas`, and `buildSpendAnalytics`. Use the current stack and current render pipeline only; no Dexie, no new store, no framework. `normalizeAnalyticsTransactions()` must map card rows to `monthLabel = fatura` and account rows to `monthLabel = mes`. `isSpendTransaction()` must reject derived/context rows, account entries that are not `saida`, and account entries categorized as `Fatura Crédito` per the phase constraint. `buildSpendAnalytics()` must return a UI-ready model containing sorted `months`, ordered `categories`, `totalsByMonth`, trend datasets, a latest-vs-previous `movers` array, and a quality object describing `Outros` share/note. Keep `Outros` in the output instead of hiding it.</action>
  <verify>
    <automated>node --test tests/phase-02/analytics-months.test.js tests/phase-02/analytics-aggregation.test.js tests/phase-02/analytics-delta.test.js</automated>
  </verify>
  <done>The helper module is fully test-backed, all Phase 2 analytics tests pass, and the output contract is ready for the Lançamentos UI to consume directly.</done>
</task>

</tasks>

<verification>
Run the Phase 2 analytics suite first, then the existing Phase 1 suite to confirm no utility regressions leaked across shared helpers:

```bash
node --test tests/phase-02/analytics-months.test.js tests/phase-02/analytics-aggregation.test.js tests/phase-02/analytics-delta.test.js
node --test tests/phase-01/*.test.js tests/phase-02/*.test.js
```
</verification>

<success_criteria>
- `js/utils/analytics.js` exists with the named exports planned above.
- Phase 2 helpers compute month/category totals from imported data only.
- Account-side `Fatura Crédito` is excluded from spend totals.
- Latest-vs-previous movers are deterministic and sorted by absolute change.
- Honest `Outros` quality metadata is available for the UI.
- `node --test tests/phase-01/*.test.js tests/phase-02/*.test.js` passes.
</success_criteria>

<output>
After completion, create `.planning/phases/02-analytics-foundation/02-01-SUMMARY.md`
</output>
