---
phase: 02-analytics-foundation
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - index.html
  - css/styles.css
  - js/app.js
  - js/views/lancamentos.js
autonomous: true
requirements:
  - ANLY-01
  - ANLY-02

must_haves:
  truths:
    - "User can see a multi-month category trend chart inside Lançamentos using imported card + account spending."
    - "User can identify which categories increased or decreased versus the prior imported month, with clear signed deltas."
    - "Existing free-text transaction search in Lançamentos still narrows the table in real time and is not redesigned by this phase."
    - "If categorization quality is weak, the analytics panel admits that `Outros` is large instead of hiding it."
  artifacts:
    - path: "index.html"
      provides: "Dedicated analytics panel containers in the Lançamentos tab"
      contains: "lancamentosAnalyticsPanel"
    - path: "js/views/lancamentos.js"
      provides: "Analytics panel rendering, chart lifecycle, movers rendering, and quality note display"
      contains: "context.analytics"
    - path: "js/app.js"
      provides: "Single-pass analytics computation during refreshDashboard render flow"
      contains: "buildSpendAnalytics"
    - path: "css/styles.css"
      provides: "Visual treatment for analytics cards, movers, and quality warning states"
  key_links:
    - from: "js/app.js"
      to: "js/utils/analytics.js"
      via: "buildSpendAnalytics import and initLancamentos context payload"
      pattern: "buildSpendAnalytics"
    - from: "js/views/lancamentos.js"
      to: "index.html"
      via: "analytics panel DOM ids and Chart.js canvas lookup"
      pattern: "lancamentosAnalytics"
    - from: "js/views/lancamentos.js"
      to: "existing search/filter flow"
      via: "filterLancamentos remains table-focused"
      pattern: "filterLancamentos"
---

<objective>
Render the Phase 2 analytics UI inside the existing Lançamentos tab and wire it into the current refresh pipeline without changing the app stack or search behavior.

Purpose: Ship the visible user value for ANLY-01 and ANLY-02 on the correct surface—unified imported history in Lançamentos—while preserving the current tab contracts and keeping Extrato account-specific.

Output: A precomputed analytics panel in Lançamentos with a multi-month category chart, latest-vs-previous movers, and an honest `Outros` quality note.
</objective>

<execution_context>
@C:/Users/Admin/.copilot/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-analytics-foundation/02-RESEARCH.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@copilot-instructions.md
@index.html
@css/styles.css
@js/app.js
@js/views/lancamentos.js
@js/views/extrato.js
@js/utils/analytics.js

<interfaces>
Expected from js/utils/analytics.js (created in Plan 02-01):
```javascript
export function buildSpendAnalytics({ lancamentos = [], extratoTransacoes = [] }) {
  return {
    months,
    categories,
    totalsByMonth,
    trendDatasets,
    latestMonth,
    previousMonth,
    movers,
    quality,
    totalSpend
  };
}
```

Current Lançamentos contract from js/views/lancamentos.js:
```javascript
export function initLancamentos(lancamentos, extratoTransacoes = [], _assinaturas = [], _despesasFixas = [], context = {}) {
  // existing context keys:
  // context.cardBillSummaries
  // context.registratoContextRows
}
```

Current Lançamentos DOM contract from index.html:
```html
<input id="searchInput" oninput="filterLancamentos()">
<select id="filterFatura" onchange="filterLancamentos()"></select>
<select id="filterCat" onchange="filterLancamentos()"></select>
<select id="filterOrigem" onchange="filterLancamentos()"></select>
<select id="filterCanal" onchange="filterLancamentos()"></select>
<select id="filterTipo" onchange="filterLancamentos()"></select>
<div id="lancamentosContextPanel"></div>
<tbody id="lancamentosTable"></tbody>
<div id="lancamentosCount"></div>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the analytics shell to the Lançamentos tab and render it from precomputed data</name>
  <files>index.html, css/styles.css, js/views/lancamentos.js</files>
  <action>Add a new analytics panel near the top of `tab-lancamentos`, above the existing filters/table, with stable IDs for a summary area, one Chart.js canvas, a movers container, and a quality note container. In `js/views/lancamentos.js`, extend `initLancamentos()` to accept `context.analytics`, store/destroy a Chart.js instance safely, and render: (1) a multi-month stacked category trend chart using all available imported months, (2) a latest-vs-previous movers surface sorted by absolute delta, with red emphasis for increases, green emphasis for decreases, and explicit labels such as `novo gasto` / `zerado` when percentage math is not meaningful, and (3) an honest quality message when `analytics.quality.shouldWarn` is true. Keep analytics in Lançamentos only; do not move or duplicate this work into `js/views/extrato.js`. Do not change the IDs or behavior of `searchInput`, `filterFatura`, `filterCat`, `filterOrigem`, `filterCanal`, or `filterTipo`. Avoid recomputing analytics inside `filterLancamentos()`; the table can keep filtering in real time, but the analytics panel must use the model computed once during refresh.</action>
  <verify>
    <automated>node --input-type=module -e "await import('./js/utils/analytics.js'); await import('./js/views/lancamentos.js');" && node --test tests/phase-01/*.test.js tests/phase-02/*.test.js</automated>
  </verify>
  <done>The Lançamentos tab has a dedicated analytics panel with chart, movers, and quality note containers; `initLancamentos()` can render them from `context.analytics`; and the existing search/filter DOM contract remains intact.</done>
</task>

<task type="auto">
  <name>Task 2: Wire unified analytics into the existing refreshDashboard render pass</name>
  <files>js/app.js</files>
  <action>Import `buildSpendAnalytics` from `js/utils/analytics.js` and compute the analytics model once inside `renderDashboard()` from the already normalized `lancamentos` and current `extratoTransacoes`. Pass that model into `initLancamentos()` through the existing context object alongside `cardBillSummaries` and `registratoContextRows`. Stay on the current stack and render pipeline: no new store, no new backend call, no change to `window` public APIs, and no shift of analytics into Extrato-only surfaces. Preserve the current free-text search behavior by leaving `filterLancamentos()` as the list filter path and not coupling chart updates to each keystroke.</action>
  <verify>
    <automated>node --input-type=module -e "globalThis.window = {}; globalThis.document = { body: { innerHTML: '' }, getElementById(){ return null; }, querySelectorAll(){ return []; } }; await import('./js/app.js');" && node --test tests/phase-01/*.test.js tests/phase-02/*.test.js</automated>
  </verify>
  <done>`app.js` computes analytics once per refresh and passes it into `initLancamentos()` without introducing new persistence, changing the tab contract, or breaking the existing free-text search flow.</done>
</task>

</tasks>

<verification>
Automated:
```bash
node --input-type=module -e "await import('./js/utils/analytics.js'); await import('./js/views/lancamentos.js');"
node --input-type=module -e "globalThis.window = {}; globalThis.document = { body: { innerHTML: '' }, getElementById(){ return null; }, querySelectorAll(){ return []; } }; await import('./js/app.js');"
node --test tests/phase-01/*.test.js tests/phase-02/*.test.js
```

Manual smoke expectation for executor before summary:
- Open the app locally.
- Confirm the new analytics panel appears in Lançamentos, not Extrato.
- Confirm table search still narrows rows in real time without rerendering/destroying the analytics panel on each keypress.
</verification>

<success_criteria>
- Lançamentos contains a new analytics panel above the existing filters/table.
- The chart reflects multi-month category spend from imported card + account data.
- The movers surface compares the latest imported month to the previous one with signed deltas.
- Account-side `Fatura Crédito` is absent from the spend analytics totals.
- `Outros` is never hidden; high `Outros` share shows a visible honesty note.
- Existing free-text search in Lançamentos still works as a table filter and was not redesigned.
- `node --test tests/phase-01/*.test.js tests/phase-02/*.test.js` passes.
</success_criteria>

<output>
After completion, create `.planning/phases/02-analytics-foundation/02-02-SUMMARY.md`
</output>
