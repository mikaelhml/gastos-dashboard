---
phase: 02-analytics-foundation
plan: "03"
type: execute
wave: 3
depends_on:
  - "02"
files_modified: []
autonomous: false
requirements:
  - ANLY-01
  - ANLY-02

must_haves:
  truths:
    - "A real user can visually identify category distribution across multiple months from the Lançamentos analytics panel."
    - "A real user can interpret which categories rose or fell versus the prior month using visible delta cues."
    - "Existing free-text search still narrows the transaction table after the analytics UI lands."
  artifacts:
    - path: "index.html"
      provides: "Lançamentos analytics panel surface for browser verification"
    - path: "js/views/lancamentos.js"
      provides: "Rendered chart, movers, and quality note behavior to validate"
    - path: "js/app.js"
      provides: "Refresh-time analytics wiring being exercised in browser"
  key_links:
    - from: "Browser Lançamentos tab"
      to: "chart + movers panel"
      via: "real imported months rendered through initLancamentos(context.analytics)"
      pattern: "lancamentosAnalytics"
    - from: "searchInput"
      to: "lancamentosTable"
      via: "filterLancamentos oninput"
      pattern: "searchInput"
---

<objective>
Run the final browser-facing verification for Phase 2 so execution stops only after the analytics UX is confirmed with real imported data and the existing search behavior still works.

Purpose: UI analytics are only done when a human confirms the chart, movers, and search compatibility on the actual app surface.

Output: A blocking approval checkpoint or a concrete issue list for follow-up fixes.
</objective>

<execution_context>
@C:/Users/Admin/.copilot/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-analytics-foundation/02-RESEARCH.md
@index.html
@js/app.js
@js/views/lancamentos.js
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Validate the Phase 2 analytics UX in the browser</name>
  <what-built>Unified Lançamentos analytics panel with a multi-month category chart, latest-vs-previous movers, and unchanged free-text search/filter workflow.</what-built>
  <how-to-verify>
    1. Start the local app using the repo’s normal local server flow (`serve.bat` on Windows, or any static server already used for this project).
    2. Open the dashboard in a browser with real imported months present.
    3. Go to the **Lançamentos** tab and confirm the analytics panel appears above the existing filters and transaction table.
    4. Confirm the trend chart covers multiple imported months and shows category distribution across those months.
    5. Compare the latest imported month against the previous month in the movers section and verify increases/decreases are visibly distinct (amount and percentage or explicit fallback labels like `novo gasto` / `zerado`).
    6. If `Outros` is large in the dataset, confirm the UI says so visibly instead of hiding it or collapsing it away.
    7. Type into the existing free-text search field and confirm the table narrows in real time exactly as before; the analytics panel should remain stable and should not feel like it is being redesigned through the search interaction.
    8. Confirm the Extrato tab still behaves as an account-specific surface and was not turned into the primary analytics home for this phase.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or reply with a numbered issue list matching the failing verification steps.</resume-signal>
</task>

</tasks>

<verification>
Automated smoke checks to run before asking for human approval:

```bash
node --input-type=module -e "await import('./js/utils/analytics.js'); await import('./js/views/lancamentos.js');"
node --input-type=module -e "globalThis.window = {}; globalThis.document = { body: { innerHTML: '' }, getElementById(){ return null; }, querySelectorAll(){ return []; } }; await import('./js/app.js');"
node --test tests/phase-01/*.test.js tests/phase-02/*.test.js
```
</verification>

<success_criteria>
- Human confirms the analytics panel is in Lançamentos and visually understandable with real imported months.
- Human confirms movers clearly show what increased/decreased versus the prior month.
- Human confirms any high `Outros` share is exposed honestly.
- Human confirms existing Lançamentos free-text search still works after the analytics UI was added.
</success_criteria>

<output>
After completion, create `.planning/phases/02-analytics-foundation/02-03-SUMMARY.md`
</output>
