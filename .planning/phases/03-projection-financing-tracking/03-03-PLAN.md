---
phase: 03-projection-financing-tracking
plan: "03"
type: execute
wave: 3
depends_on:
  - "02"
files_modified: []
autonomous: false
requirements:
  - PROJ-01
  - PROJ-02

must_haves:
  truths:
    - "A real user can see which SCR-derived commitments were included automatically versus blocked as conflict/context only."
    - "A real user can confirm that only safe SCR commitments alter the projection totals."
    - "A real user can see that installment/financing impact is summarized in Projeção while the detailed tracker remains in Despesas & Parcelas."
    - "A real user can navigate from Projeção to the detailed review surfaces using the provided shortcuts."
  artifacts:
    - path: "index.html"
      provides: "Browser-visible Projeção surface with dedicated SCR section and compact tracker-impact area"
    - path: "js/views/projecao.js"
      provides: "Rendered status rows, separate SCR math bucket, and tracker shortcut behavior"
    - path: "js/app.js"
      provides: "Refresh-time projection model wiring exercised during smoke validation"
    - path: "tests/phase-03/projection-model.test.js"
      provides: "Automated regression check for inclusion/conflict/contextual semantics"
    - path: "tests/phase-03/parcelamento-summary.test.js"
      provides: "Automated regression check for compact tracker summary semantics"
  key_links:
    - from: "Browser Projeção tab"
      to: "SCR commitments section"
      via: "dedicated containers rendered before scenarios/table"
      pattern: "projecaoScrPanel"
    - from: "SCR commitment status rows"
      to: "projection math"
      via: "only included rows contribute to scrIncluido"
      pattern: "scrIncluido"
    - from: "Projeção shortcut buttons"
      to: "Despesas & Parcelas / Registrato"
      via: "existing switchTab calls"
      pattern: "switchTab"
---

<objective>
Run the final smoke and browser verification for Phase 3 so execution stops only after the user confirms the SCR inclusion policy, explainability surfaces, and tracker-placement boundaries on the real app.

Purpose: Phase 3 is only complete when automated checks pass and a human verifies that Projeção is trustworthy, explicit about uncertainty, and still respects the existing Despesas & Parcelas ownership of detailed tracking.

Output: A blocking approval checkpoint or a concrete issue list tied to visible verification steps.
</objective>

<execution_context>
@C:/Users/Admin/.copilot/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-projection-financing-tracking/03-CONTEXT.md
@.planning/phases/03-projection-financing-tracking/03-RESEARCH.md
@index.html
@js/app.js
@js/views/projecao.js
@js/views/parcelamentos.js
@js/views/registrato.js
@js/utils/projection-model.js
@js/utils/parcelamento-summary.js
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Validate the Phase 3 projection and financing UX in the browser</name>
  <what-built>Projeção now consumes a dedicated SCR projection model, shows explicit `included` / `conflict` / `contextual-only` commitment states before the projection table, uses a separate `SCR incluído` math bucket, applies payoff-aware scheduling, and shows only compact tracker impact plus shortcuts instead of duplicating the detailed tracker.</what-built>
  <how-to-verify>
    1. Start the local app using the repo’s normal local server flow (`serve.bat` on Windows, or the same static server workflow already used in prior phases).
    2. Open the dashboard in a browser with imported Registrato/SCR data present and at least one dataset containing manual fixed expenses and/or parcelamentos.
    3. Go to the **Projeção** tab and confirm a dedicated SCR-derived commitments section appears before the scenarios and before the month-by-month projection table.
    4. Confirm that the section distinguishes the three required states explicitly: one visual treatment for `included`, one for `conflict`, and one for `contextual-only` per D-05 and D-06.
    5. Confirm that rows with weak or aggregated SCR evidence are still visible as context and are not silently injected into the math per D-02.
    6. Confirm that any SCR row that appears to overlap a manual fixed expense or financing item is presented as a conflict/review case and is not auto-included per D-03 and D-04.
    7. Change the projection horizon (`6`, `12`, `24` meses), click **Recalcular Projeção**, and confirm the monthly table shows a separate `SCR incluído` bucket while future months stop charging ended manual parcelas/financiamentos instead of keeping them flat forever.
    8. Confirm the monthly result banner and charts reflect included SCR impact only; contextual/conflict rows should not alter totals.
    8a. Confirm the scenario cards and `Ponto de Equilíbrio` are consistent with the recalculated projection model (including payoff-aware manual items and the separate `SCR incluído` bucket), or are explicitly labeled if they are not part of the recalculated output.
    9. Confirm Projeção shows only compact financing/installment impact summary plus shortcuts, and that the detailed `Despesas & Parcelas` tracker still contains the full financing/installment cards and detail table per D-07 and D-08.
    10. Use the Projeção shortcuts to open **Despesas & Parcelas** and **Registrato** and confirm they navigate correctly with the existing tab system.
    11. Sanity-check the double-count guard: no card-bucket-risk SCR commitment should feel auto-added on top of the user-entered `Fatura do cartão/mês`.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or reply with a numbered issue list matching the failing verification steps.</resume-signal>
</task>

</tasks>

<verification>
Automated smoke checks to run before asking for human approval:

```bash
node --test tests/phase-03/projection-model.test.js tests/phase-03/projection-conflicts.test.js tests/phase-03/parcelamento-summary.test.js
node --test tests/phase-01/*.test.js tests/phase-02/*.test.js tests/phase-03/*.test.js
node --input-type=module -e "await import('./js/utils/projection-model.js'); await import('./js/utils/parcelamento-summary.js'); await import('./js/views/projecao.js');"
node --input-type=module -e "globalThis.window = {}; globalThis.document = { body: { innerHTML: '' }, getElementById(){ return null; }, querySelectorAll(){ return []; } }; await import('./js/app.js');"
node --input-type=module -e "import fs from 'node:fs'; const html = fs.readFileSync('./index.html', 'utf8'); if (!html.includes('projecaoScrPanel')) throw new Error('Missing projecaoScrPanel container'); if (!html.includes('SCR incluído')) throw new Error('Missing SCR incluído surface in projection HTML');"
```
</verification>

<success_criteria>
- Automated Phase 3 smoke checks pass before the checkpoint is shown.
- Human confirms the dedicated SCR section appears before the projection table and clearly shows included/conflict/contextual-only states.
- Human confirms only safe included commitments affect projection totals.
- Human confirms month-aware payoff behavior is visible in future months.
- Human confirms Projeção keeps only summary/impact plus shortcuts and does not duplicate the detailed tracker.
</success_criteria>

<output>
After completion, create `.planning/phases/03-projection-financing-tracking/03-03-SUMMARY.md`
</output>
